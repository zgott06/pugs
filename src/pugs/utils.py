import pandas as pd

def trim(df: pd.DataFrame, start_time: float = None, method: str = "threshold", **kwargs) -> pd.DataFrame:
    pass

def stats(df: pd.DataFrame, variable: str | list = None, start_time: float = None) -> dict:
    pass